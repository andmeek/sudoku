import Generate from '../../src/models/generate.js'

describe('Generate', () => {
  var generator, seed = 1

  beforeEach(() => {
    generator = new Generate(seed)
  })

  describe('.board', () => {

  })

  describe('.boardState', () => {

  })
})
